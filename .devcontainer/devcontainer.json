{
  "name": "sv-uvm-example",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.makefile-tools",
        "eirikpre.systemverilog",
        "wavetrace.wavetrace"
      ]
    }
  },
  "postCreateCommand": "verilator --version; iverilog -V | head -n 1"
}
